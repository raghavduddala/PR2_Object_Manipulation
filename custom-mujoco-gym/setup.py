from setuptools import setup

setup(name="custom_mujoco_gym",
      version="0.17.3",
      install_requires = ["gym"]
#       #and any other dependencies should be added here
#       #for software package versions in conda, do:
#       # $ conda list
      )